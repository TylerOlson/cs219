Programming Project Part 1 Part 3 CS 219 - Computer Organization Fall 2023

This program has the following commands: ADD, AND, ASR, LSR, LSL, NOT, ORR, SUB, XOR, MOV, TST, CMP.
Supply an S after to save N and Z flags.

The input file will be formatted: "CMD <number1> <number2> <number2>" with numbers being either hexadecimal or a register value.
You can choose to omit <number3> in commands that require only one number.

Instructions: Run `make` to build the binary.
Run the program output executable in terminal.